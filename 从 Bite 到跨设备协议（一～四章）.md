# 第一章：一个数据在计算机里到底是什么？

> 在 C 语言里写下 `10`、`0x0A`、`'A'`、`"10"` 时，计算机内部到底发生了什么

---

## 先从一个非常重要的结论开始

比如写：`uint8_t a = 10;`

> 这并不是在内存里面存了一个“10”
>

内存里最终只有一组 bit：`00001010`

至于你把这组 bit 看成：

```text
十进制：10

十六进制：0x0A

二进制：0b00001010
```

只是**人类对同一份数据的不同表示方式**。

所以第一条核心原则：

> [!important] 核心原则
> **数据和数据的表示方法，是两回事。**
>

---

## bit / Byte

### bit

> 抽象来看，一个 bit 只有两种状态：`0` 和 `1`
>
> 实际硬件里面，它最终可能通过：
>
> ```text
> 高/低电平
> 晶体管状态
> 电荷状态
> 磁性状态
> ```
>
> 等物理方式保存。
>
> 但是软件层面，我们统一抽象成：`0 / 1`
>

---

### Byte

> 是我们真正经常操作的基本单位
>
> 每一个 Byte 有 8 个 bit，例如：
>
> ```c
> uint8_t buf[] = {
>     0x12,
>     0x34,
>     0xAB,
>     0xFF
> };
>
> /*
>  * 本质就是：
>  *  Byte0       Byte1       Byte2       Byte3
>  *  00010010    00110100    10101011    11111111
>  */
> ```
>

---

## 为什么嵌入式特别喜欢 Hex？

> 因为二进制太长，人很难快速阅读。而十六进制有一个极其漂亮的性质：
>
> **1 个十六进制数字正好对应 4 bit ：**
>
> 所以：
>
> ```text
> 0000 = 0    0001 = 1    0010 = 2    0011 = 3
> 0100 = 4    0101 = 5    0110 = 6    0111 = 7
> 1000 = 8    1001 = 9    1010 = A    1011 = B
> 1100 = C    1101 = D    1110 = E    1111 = F
>
> # Hex 更多是：
> #     **人类查看和书写二进制数据的一种方便方式。**
> **#【Hex 是人观察二进制最方便的方法之一。】**
> ```
>

---

## 现在看一个非常关键的 C 语言例子

> ```c
> uint8_t a = 10;
> uint8_t b = 0x0A;
> uint8_t c = 0b00001010;
>
> /*
>  * 这里的 **a、b、c** 没有任何区别
> ** * 变量最终保存的数据完全一样，**都是：00001010
> ** * 区别只是程序员写源代码时选择了不同的“表示法”。**
>  */
> ```
>
> 编译器看到它们以后，表示方式就消失了
>
> 源代码：`uint8_t a = 10;` 或者 `uint8_t a = 0x0A;`
>
> 编译以后，对于数据本身来说最终没有：
>
> “十进制属性”  或者  “十六进制属性”
>
> 内存只保存：`00001010`
>

> **真正完整的思想是：**
>
> ```text
> bit 本身
>     ↓
> 没有天然意义
>     ↓
> 按照某种规则解释
>     ↓
> 整数 / 字符 / 指令 / 浮点数 / 像素 / 音频……
> ```
>

> **这也就是数据为什么一定需要“规则”**
>
> 假设 MCU A 给 MCU B 发送：
>
> ```text
> 01 02
> ```
>
> MCU B 怎么知道是什么意思？
>
> 可能是 情况 A：
>
> ```text
> 两个 uint8_t
>
> 1
> 2
> ```
>
> 也可能是 情况 B：
>
> ```text
> 一个 uint16_t
>
> 0x0102
> ```
>
> 也可能 情况 C：
>
> ```text
> CMD = 0x01
> LEN = 0x02
> ```
>
> 也可能 情况 D：
>
> ```text
> Type   = 0x01
> Length = 0x02
> ```
>
> 甚至可能：
>
> ```text
> 01 02
>
> # 是图片中的两个像素。
> ```
>
> 所以：**Byte 自己不会告诉你“我是什么意思”。**
>
> 必须双方提前约定：**如何解释这些 Byte**
>
> 这个约定以后就是：
>
> ```text
> 数据类型
> 编码规则
> 协议
> 文件格式
> 序列化格式
> ...
> ```
>

---

## 整个的核心

> 以后：
>
> ```text
> Base64
> ASN.1
> DER
> PEM
> TLV
> JSON
> Protobuf
> ```
>
> 本质上都在干类似的事情：
>
> **制定规则，让一串 byte 能够被准确地解释。**
>
> 只是它们解决的问题层次不同。
>

---

## C 中的 `0x12` 和 `"12"` 完全不是一回事

> ```c
> uint8_t a = 0x12;
>
> char *b = "12";
>
> uint8_t c = 12;
>
> char d = '1';
> ```
>
> ```text
> # a：
>     一个 Byte
>     12
>
> # b:
>     指针
>     指向字符串：31 32 00
>
> # c:
>     一个 Byte
>     0c
>
> # d:
>     一个 Byte
>     31
> ```
>
> 所以：
>
> ```text
> 0x12 与 "12" 与 12
> 完全不是一回事
> ```
>

---

## `printf` 能非常好地证明“表示”和“数据”是两回事

> ```c
> uint8_t a = 165;
>
> // 然后
> printf("%u\n", a);
> printf("%02X\n", a);
> ```
>
> 输出：
>
> ```text
> 165
> A5
>
> # 内存还是同样的：10100101
> # 只是不同的 格式说明符
> # 显示的不同（人为需要），数据本身没变
> # 这里的 %02X 种的 02 表示：至少输出两位，不足两位前面补 0。
> ```
>

---

## 一个 `uint32_t` 不是一个“大格子”

> 比如：
>
> ```c
> uint32_t value = 0x12345678;
> ```
>
> 逻辑上：
>
> ```text
> value = 32 bit
> ```
>
> 但是内存仍然按 Byte 编址占 *4 个连续地址 ，*类似：
>
> ```text
> 地址             Byte
>
> 0x20000000       ??
> 0x20000001       ??
> 0x20000002       ??
> 0x20000003       ??
>
> # 至于这里究竟是：
> # 12 34 56 78    还是    78 56 34 12
>
> # 就涉及：**  大小端**
> # 这正是我们第三阶段会重点研究的内容。
> ```
>

---

## 文件本质上也只是 Byte

> 这个认知也非常重要，比如：
>
> ```text
> test.bin
>
> # 在磁盘上：
> # Byte Byte Byte Byte Byte...
> ```
>

`test.txt` 、`JPEG`、`PEM`、`ELF`、本质也一样：

```text
Byte Byte Byte Byte Byte...
```

所以：

> **从物理存储角度看，所有文件最终都是 Byte 序列。**
>

区别只在于：

> **使用什么规则解释这些 Byte。**
>

> 例如：
>
> ```text
> 48 65 6C 6C 6F
>
> # 按照 ASCII / UTF-8：
> # Hello
> # 按照别的协议，则可能有完全不同的意义。
> ```
>

---

## 通信链路本质也一样

> **整个系统世界可以抽象成：**
>
> ```text
> 内存中的 Byte
>       ↓
> 编码 / 组织
>       ↓
> 发送 Byte
>       ↓
> 链路
>       ↓
> 接收 Byte
>       ↓
> 解析
>       ↓
> 另一个设备中的 Byte
> ```
>

---

## 一个非常重要的“数据世界观”

> 以后看到任何东西：
>
> ```text
> SM4 Key
> SM2 Public Key
> Certificate
> JSON
> PEM
> Firmware
> Image
> Protocol Packet
> ```
>

> 先不要害怕格式，第一反应：
>
> **把它剥到底层，它一定是一串 Byte。**
>

> 例如未来我们会看到：
>
> ```text
> -----BEGIN CERTIFICATE-----
> MIIC...
> -----END CERTIFICATE-----
> ```
>
> 不要感觉它神秘，一层层扒：
>
> ```text
> PEM 文本
> ↓
> Base64
> ↓
> DER Byte
> ↓
> ASN.1 TLV
> ↓
> Certificate 数据结构
> ↓
> 公钥、Issuer、Subject……
> ```
>
> 扒到底：**还是 Byte。**
>


好，我们继续《系统中的数据表达、编码与传输：从 Bit 到跨设备协议》的**第二课**。

# 第二章：同一串 Bit，为什么可以代表完全不同的东西？

这一课非常关键，因为从这里开始，你要建立一个贯穿整个课程的核心认识：

> [!important] 本章核心
> **内存里没有“整数”“负数”“字符”这些东西。**
>
> 内存里只有 **Bit**。
>
> `signed / unsigned / ASCII / UTF-8`，本质上都是：
>
> **“我们约定如何解释这些 Bit。”**
>

---

## 一、先看一个最重要的例子

> 假设 MCU 内存里有一个字节：
>
> ```text
> 1111 1111
>
> 也就是：
>     Binary = 11111111
>     Hex    = 0xFF
> ```
>
> 问题来了：**它是多少？**
>
> 答案不是唯一的。
>
> ```text
> # 如果按照 uint8_t 解释：
>
> uint8_t a = 0xFF;
>
> a = 255
>
> # 但是如果按照 int8_t 解释：
>
> int8_t b = 0xFF;
>
> b = -1
> ```
>
> ```text
> 内存中的 Bit：
>
> 11111111
> ```
>
> **完全没有发生变化。**
>
> 变化的只是：**解释规则**
>
> ```text
> 11111111
>               │
>               │
>        ┌──────┴──────┐
>        │             │
>    unsigned       signed
>        │             │
>       255           -1
> ```
>
> 这就是这一课最重要的思想。
>

---

## 二、unsigned 是什么意思？

> `unsigned`：无符号整数（ *所有 Bit 都拿来表示数值。*）
>
> ```text
> b7   b6  b5  b4  b3  b2  b1  b0
>
> 128  64  32  16  8   4   2   1
> ```
>
> 例如：
>
> ```text
> 0101 1010
>
> # 计算：
> #     0×128 + 1×64 + 0×32 + 1×16 + 1×8 + 0×4 + 1×2 + 0×1 = 90
> ```
>
> 所以 unsigned 能表示的范围：
>
> ```text
> uint8_t   ~  255
> uint16_t  ~  65535
> uint32_t  ~  4294967295
> uint64_t  ~  2⁶⁴-1
> ```
>

---

## 三、那 signed 怎么表示负数？

> ```c
> int8_t a;
>
> /*
>  * 同样只有：
>  *     8 Bit
>  *
>  * 但是它要表示：
>  *     正数  0  负数
>  *
>  * 怎么办？现代计算机基本统一使用：
>  *    二进制补码（ Two's Complement ）不再深入，知道就行
> ```
>
> 对于 unsigned：
>
> ```text
> 0x00 ～ 0xFF
>
> # 0  ~  255
> ```
>
> 对于 signed，我们规定：
>
> ```text
> 0x00 ~ 0x7F
>
> # 仍然表示：
> # 0 ~ 127
>
> 0x80 ~ 0xFF
>
> # 重新解释成：
> # -128 ~ -1
> ```
>
> 于是：
>
> ```text
> Hex       uint8_t      int8_t
>
> 0x00          0           0
> 0x01          1           1
> ...
> 0x7F        127         127
>
> 0x80        128        -128
> 0x81        129        -127
> ...
> 0xFE        254          -2
> 0xFF        255          -1
> ```
>

---

## 四、嵌入式里面为什么强烈推荐 uint8_t 这种类型？

> 以后写嵌入式代码，经常应该看到：
>
> ```c
> uint8_t
> uint16_t
> uint32_t
>
> int8_t
> int16_t
> int32_t
> ```
>
> 而不是大量使用：
>
> ```c
> char
> short
> int
> long
> ```
>
> 核心原因：**位宽明确**
>
> 例如：
>
> ```c
> uint32_t counter;
>
> /*
>  * 32 bit
>  * unsigned
>  */
>
>  int counter;
>
>  /*
>   * 只是说：整数
>   * 具体宽度跟平台和 ABI 有关。
>   */
> ```
>

---

## 五、这和通信协议有什么关系？

> ```c
> // 假设设备 A 发送：FF
> // 设备 A 认为字段定义是：
> int8_t temperature;        // 于是：设备 A 认为发出的是 -1°C
> ```
>
> 设备 B  ---收到--->  设备 A 发来的 *FF*
>
> ```c
> uint8_t temperature;        // 设备 B 认为 收到的 FF 是 255°C
> ```
>
> 链路传输完全正确。
>
> ```text
> UART 没错
> I2C 没错
> SPI 没错
> CRC 也没错
> ```
>
> 结果却彻底错误。
>
> 这就是：**协议语义不一致**
>
> 所以协议不能只写：
>
> ```text
> temperature: 1 byte
> ```
>
> 更规范的是：
>
> ```text
> temperature
>
> Length : 1 byte
> Type   : int8_t
> Unit   : °C
> Range  : -40 ~ 125
> ```
>

---

## 六、接下来进入另一个核心问题：字符

> 假设内存里：
>
> ```c
> 0100 0001
> /*
>  * 也就是 0x41
>  * 如果按照整数解释：65
>  * 但如果按照 ASCII 解释：'A'
>  *
>  * 还是这句话：Bit 没变，解释规则变了。
>  */
> ```
>

---

## 七、那中文怎么办？

> ASCII 只能很好地处理：
>
> ```text
> 英文
> 数字
> 常用符号
> 控制字符
> ```
>
> 不能直接表示：
>
> ```text
> 中
> 国
> 你
> 好
> ```
>
> 于是有了扩展统一的 **Unicode**
>
> ```text
> Unicode 给每个字符分配一个 **唯一的编号**，叫做 **码点 (code point)**。
> # 这是写在 “字典” 里面的规则，并不是实际存储
>
> # 最常见的存储时 UTF-8
> ```
>
> 比如字符 "中"：
>
> ```c
> 中
> Unicode 编号：U+4E2D
>
> // Unicode 更像：全球字符身份证号码系统
> ```
>
> - UTF-8 负责：这个 Unicode 编号到底怎么变成字节，怎么存储
>
>     ```text
>     也就是说：
>     # Unicode：字符编号体系
>     # UTF-8：字节编码方式
>
>     # 于是，经过 UTF-8 编码后：
>     E4 B8 AD
>     也就是说使用 UTF-8 字符 "种" 需要 3Byte
>     ```
>
> 也就是我们最应该记住的要点：
>
> 当MCU 收到：`31 32 33`
>
> 我们最先考虑的是 ---> ***要用什么规则来解释***
>
> - **链路上传输的是 Byte，Byte 的意义由协议决定。**
>

---

# 第三章：大小端、内存布局、对齐与 Padding

> 真正进入“**C 语言看到的数据**”和“**内存里实际放的数据**”之间的关系。
>

这一课你学完以后，应该能彻底理解这些问题：

- 为什么 `0x12345678` 在内存里可能是 `78 56 34 12`

- 为什么串口协议里经常规定“大端”或“小端”

- 为什么 `uint8_t *p = (uint8_t *)&a;` 可以看到一个整数的真实字节

- 为什么一个 `struct` 明明成员加起来 5 字节，`sizeof()` 却可能是 8

- 为什么不同 MCU / CPU / 编译器之间直接传 `struct` 很危险

- 为什么网络协议、文件格式、密码算法都必须明确字节序

> [!important] 工程原则
> **跨设备交换的应该是“明确规定的字节格式”，而不是某台机器的内部内存布局。**

---

## 一、大端 / 小端

> ```c
> uint32_t a = 0x12345678;
> ```
>
> 我们知道：
>
> ```text
> uint32_t = 32 bit = 4 byte
>
> 那么 a 就是这四个字节：
>     0x12
>     0x34
>     0x56
>     0x78
> ```
>
> 大小端就是规定这四个字节的存储顺序
>
> **大端 Big-Endian：**
>
> ```c
> **// 高位字节放在低地址。**
>
> 假设 uint32_t a = 0x12345678;
> 内存地址从：0x1000 开始
>
> 那么大端存储就是：
>
>     低地址                        高地址
>       ↓                             ↓
>     0x1000    0x1001    0x1002    0x1003
>     12        34        56        78
> // 计算机按内存寻址，一个地址存放一个字节 ⭐⭐⭐
> ```
>
> **小端 Little-Endian**
>
> ```c
> **// 低位字节放在低地址。**
>
> 那么小端存储就是：
>
>     低地址                        高地址
>       ↓                             ↓
>     0x1000    0x1001    0x1002    0x1003
>     78        56        34        12
>
> /*
>  * 常见的：
>  *   ARM Cortex-M
>  *   x86
>  *   x86-64
>  * 都是小端
> ```
>

---

## 二、强转成 uint8_t * 能看到真实内存？

> ```c
> uint32_t a = 0x12345678;
>
> uint8_t *p = (uint8_t *)&a;
>
> // &a ：a 的起始地址
> // 假设 &a = 0x1000
> ```
>
> 原来：
>
> ```c
> uint32_t *
>
> // 意味着 把这里看成一个 32-bit 整数
> ```
>
> 现在转成：
>
> ```c
> uint8_t *
>
> // 意味着 我从这个地址开始，一个 byte 一个 byte 地看。
> ```
>
> 于是：
>
> ```c
> // 如果是小端：
> p[0] = 0x78
> p[1] = 0x56
> p[2] = 0x34
> p[3] = 0x12
>
> // 就是内存里的 4 个真实字节。
> ```
>

---

## 三、什么要关心大端

> 因为：**本机内存格式和通信格式不是一回事。**
>
> 这是一个非常重要的概念。
>
> 例如：
>
> - MCU 是：**Little-Endian**
>
> - 但是通信协议规定：**16-bit Length 使用 Big-Endian**
>
> 于是假设有 `length = 0x1234`，其在内存中：**34  12**
>
> 但是在线路上必须发送：**12  34**
>
> ```text
> ***CPU 内存字节序***  和  ***协议字节序***
>
> 必须区分开
> ```
>

---

## 四、一个非常真实的通信 Bug

> 假设主机发送长度：
>
> ```text
> 0x0102    # 也就是 258
> ```
>
> 协议规定：**Big-Endian**
>
> 所以发送：
>
> ```text
> 01 02
> ```
>
> MCU 收到，放到内存里：
>
> ```c
> uint8_t buf[2];
>
> buf[0] = 0x01;
> buf[1] = 0x02;
> ```
>
> 如果错误地这么解析：
>
> ```c
> uint16_t len = *(uint16_t *)buf;
>
> /*
>  * buf 是一个指向内存的指针（ uint8_t *buf ），指向某个数据区域的起始地址
>  * (uint16_t *) 是类型转换：
>  *     把 buf（原本是 uint8_t*，指向字节）强制转换成 uint16_t*
>  *     即指向 16 位（2 字节）数据 的指针。
>  *
>  * 然后 * 解应用
>  * 解引用后，编译器会一次性读取 2 个字节（因为是 uint16_t*）。
>  * 结果就是把 buf[0] 和 buf[1] 这两个字节拼成一个 uint16_t 整数。
>  */
> ```
>
> 假设 MCU 是小端。
>
> 它会解释成：**02  01**
>
> 于是：`len = 513` ，协议瞬间错了。
>

---

> 假设我们设计协议：
>
> ```text
> 命令（cmd）：
> 0x10
>
> 温度（temperature）：
> 16-bit signed
> Big-Endian
>
> 状态（status）：
> uint8_t
> ```
>

---

## 五、正确解析协议数据

> ***数据离开本机以后，不能假设对端的字节序和你一样。***
>

> 如果协议明确规定：**Big-Endian**
>
> ```c
> uint16_t len =
>     ((uint16_t)buf[0] << 8) |
>     ((uint16_t)buf[1]);
> ```
>
> 如果协议规定：**Little-Endian**
>
> ```c
> uint16_t len =
>     (uint16_t)buf[0] |
>     ((uint16_t)buf[1] << 8);
> ```
>
> 以后主动写两个函数：
>
> ```c
> // ========= 16 ===========
> uint16_t read_be16(const uint8_t *p)
> {
>     return ((uint16_t)p[0] << 8) |
>            ((uint16_t)p[1]);
> }
>
> uint16_t read_le16(const uint8_t *p)
> {
>     return ((uint16_t)p[0]) |
>            ((uint16_t)p[1] << 8);
> }
>
> // ========= 32 ===========
> uint32_t read_be32(const uint8_t *p)
> {
>     return ((uint32_t)p[0] << 24) |
>            ((uint32_t)p[1] << 16) |
>            ((uint32_t)p[2] << 8)  |
>            ((uint32_t)p[3]);
> }
>
> ....
> ```
>

---

## 六、内存对齐 Alignment

> CPU 通常更喜欢：
>
> ```text
> 2-byte 数据放在 2 的倍数地址
> 4-byte 数据放在 4 的倍数地址
> 8-byte 数据放在 8 的倍数地址
> ```
>
> 例如：
>
> ```c
> uint32_t b;
>
> /*
>  * CPU 通常希望地址：
>  *     0x1000
>  *     0x1004
>  *     0x1008
>  *     0x100C
>  *     ...
>  *
>  * 也就是：address % 4 == 0
>  * 这就是：4-byte aligned
> ```
>
> 为什么 CPU 喜欢对齐？
>
> 简单来说就是如果不对齐，CPU 可能需要：
>
> ```text
> 读两次
> +
> 拼接
> # 效率下降
>
> # 甚至在某些架构上：
> 未对齐访问可能直接触发异常
> ```
>
> **所以编译器倾向于主动帮你把数据“摆整齐”。*****（ Padding ）***** **
>
> 比如：
>
> ```c
> struct Test
> {
>     uint8_t  a;
>     uint32_t b;
> };
>
> // 假设结构地址：0x1000
> // a 放 0x1000 占 1 Byte
>
> // 下一个地址：0x1001
> // 但是 b 是 uint32_t
> // 需要 4字节 对齐，最近的 4 的倍数地址：0x1004
>
> // 所以：0x1001、0x1002、0x1003 会被空出来
> ```
>
> 于是：
>
> ```text
> 地址        内容
>
> 0x1000      a
>
> 0x1001      padding
> 0x1002      padding
> 0x1003      padding
>
> 0x1004      b byte0
> 0x1005      b byte1
> 0x1006      b byte2
> 0x1007      b byte3
>
> # 总大小：8 Byte
> # 真实字段 = 5 Byte
> # padding = 3 Byte=
> ```
>
> **Padding** 不是定义的成员，而是：
>
> 编译器为了满足 alignment 自动插进去的空洞。
>
> ---
>
> ***成员顺序影响 struct 大小***
>
> ```c
> struct A
> {
>     uint8_t  a;
>     uint32_t b;
>     uint8_t  c;
> };
>
> /*
> 布局：
> offset 0     a
> offset 1     padding
> offset 2     padding
> offset 3     padding
>
> offset 4~7   b
>
> offset 8     c
>
> offset 9~11  tail padding
> */
>
> sizeof(struct A);    // 等于 12
> ```
>
> 但是如果重新排列：
>
> ```c
> struct B
> {
>     uint32_t b;
>     uint8_t  a;
>     uint8_t  c;
> };
>
> /*
> offset 0~3   b
> offset 4     a
> offset 5     c
> offset 6~7   padding
> */
>
> sizeof(struct B);    // 等于 8
>
> // 这就是为什么建议结构体成员 由大到小排列
> ```
>
> ---
>
> offsetof 很有用
>
> C 语言里有：`offsetof()`
>
> 例如：
>
> ```c
> #include <stddef.h>
>
> struct Test
> {
>     uint8_t  a;
>     uint32_t b;
> };
>
> printf("%zu\n", offsetof(struct Test, a));
> printf("%zu\n", offsetof(struct Test, b));
> ```
>
> 就能看到：
>
> ```text
> a offset = 0
> b offset = 4
>
> # 这非常直观的证明了 offset 1~3 是 padding
> ```
>

---

## 七、为什么不能直接把 struct 当协议发出去？

比如我们这样写：

```c
struct Packet
{
    uint8_t cmd;
    uint32_t value;
};

// 然后
uart_send((uint8_t *)&packet, sizeof(packet));
```

看起来非常方便，但有两个隐患：

```text
1. endian
2. padding
```

### 问题 1：Padding

> 想象中的协议：
>
> ```text
> cmd      1 byte
> value    4 byte
>
> 总长度 5 Byte
> ```
>
> 但结构体可能：8 Byte
>
> 实际发出去：
>
> ```text
> cmd
>
> padding
> padding
> padding
>
> value
> value
> value
> value
> ```
>
> 对端如果按 5 字节协议解析，直接错位。
>

### 问题 2：Endian

> ```c
> // 结构体 成员
> packet.value = 0x12345678;
> ```
>
> MCU 是小端，struct 内存可能是：
>
> ```text
> cmd
> xx xx xx
> 78 56 34 12
> ```
>
> 但协议规定：**是大端（ value = big endian ）**
>
> 期望：
>
> ```text
> 12 34 56 78
> ```
>
> 直接发送 struct，完全错误。
>

***并且不同平台 结构体布局 可能不同***

---

## 八、（序列化）工程上的协议写法

> 假设协议：
>
> ```text
> Byte 0      cmd
> Byte 1~4    value，Big-Endian
> ```
>
> 应该明确序列化：
>
> ```c
> buf[0] = packet.cmd;
>
> buf[1] = (uint8_t)(packet.value >> 24);
> buf[2] = (uint8_t)(packet.value >> 16);
> buf[3] = (uint8_t)(packet.value >> 8);
> buf[4] = (uint8_t)(packet.value);
> ```
>
> 这样发送：
>
> ```text
> cmd 12 34 56 78
> ```
>
> 完全不依赖：
>
> ```text
> CPU endian
> struct padding
> compiler
> ```
>
> 这就叫：**Serialization【序列化】**
>
> ---
>
> 反过来叫 **反序列化 ( Deserialization )**
>
> 收到：
>
> ```text
> cmd 12 34 56 78
> ```
>
> 解析：
>
> ```c
> packet.cmd = buf[0];
>
> packet.value =
>     ((uint32_t)buf[1] << 24) |
>     ((uint32_t)buf[2] << 16) |
>     ((uint32_t)buf[3] << 8)  |
>     ((uint32_t)buf[4]);
> ```
>
> ***于是完整过程：***
>
> ```text
> 程序内部数据结构
>         │
>         │ serialization
>         ▼
> Raw Bytes
>         │
>         │ UART / I2C / SPI / TCP
>         ▼
> Raw Bytes
>         │
>         │ deserialization
>         ▼
> 对端程序内部数据结构
> ```
>

---

## 九、packed struct

> 你可能见过：
>
> ```c
> #pragma pack(1)
>
> // 或者
>
> __attribute__((packed))
> ```
>
> 例如：
>
> ```c
> struct __attribute__((packed)) Packet
> {
>     uint8_t  cmd;
>     uint32_t value;
> };
> ```
>
> 这样编译器可能不插 padding，于是：
>
> ```c
> sizeof(Packet) = 5
>
> /* 布局：
> cmd
> value byte0
> value byte1
> value byte2
> value byte3
> */
> ```
>
> 看起来可以直接发，但还是不行，因为：
>
> 这样访问效率低
>
> 甚至可能触发 *BusFault 、HardFault*
>
> ***那 packed 有什么用***
>
> 常见在：
>
> ```text
> 硬件寄存器映射
> 文件头
> 固定二进制格式
> 协议结构映射
> Bootloader Header
> Flash 固定布局
> ```
>
> 一定要非常清楚：
>
> - *alignment*
>
> - *endianness*
>
> - *compiler behavior*
>
> 不能盲目使用
>

---

## 十、寄存器为什么通常用 uint32_t？

> 例如 MCU 外设：
>
> ```c
> typedef struct
> {
>     volatile uint32_t CTRL;
>     volatile uint32_t STATUS;
>     volatile uint32_t DATA;
> } UART_TypeDef;
> ```
>
> 因为硬件寄存器地址可能：
>
> ```text
> 0x40000000 CTRL
> 0x40000004 STATUS
> 0x40000008 DATA
> ```
>
> 都是：***4-byte aligned***
>
> 所以这种结构体正好天然匹配硬件布局。
>
> ---
>
> 这和普通通信协议 struct 不一样
>
> 因为硬件手册已经明确规定：
>
> ```text
> 每个寄存器的 offset
> ```
>
> 例如：
>
> ```text
> CTRL   offset 0x00
> STATUS offset 0x04
> DATA   offset 0x08
> ```
>
> C struct 只是拿来描述这个固定硬件布局。
>

---

## 十一、现在可以理解 Memory Map

```c
0x40000000   UART_CTRL
0x40000004   UART_STATUS
0x40000008   UART_DATA

// 然后
typedef struct
{
    volatile uint32_t CTRL;
    volatile uint32_t STATUS;
    volatile uint32_t DATA;
} UART_TypeDef;

#define UART0 ((UART_TypeDef *)0x40000000)
```

那么写：

- `UART0->STATUS`

编译器实际上就是访问：

- *0x40000004*

因为：

- STATUS offset = 4

这就是：

```text
struct layout
+
memory map
+
pointer
```

组合起来的结果。

MCU 头文件里大量这种写法，现在应该已经能理解它底层是什么了。

---

## 十二、这和 Flash 数据区也直接相关

假如你希望 Flash 里保存：

```c
struct Config
{
    uint8_t version;
    uint32_t serial;
};
```

直接：

```c
flash_write(addr, (uint8_t *)&config, sizeof(config));
```

就会有隐患：

```text
padding
compiler
version compatibility
endianness
```

更稳定的做法是定义：***Flash Binary Format***

比如：

```text
Offset   Length   Meaning

0x00     1        version
0x01     4        serial BE
0x05     2        crc
```

然后显式编码，这就是：

- **不要把“内存结构”误认为“持久化格式”。**

- **不要把“内存结构”误认为“通信格式”。**

---

## 十三、Endian 还有 Bit Endian 吗？

理论上确实还可以讨论：***bit order***

比如：

```text
MSB first
LSB first
```

但它和 `byte endian` 不是完全一回事。

例如 SPI 经常有：

```text
MSB First
LSB First
```

那讨论的是：***一个 byte 内部，哪一位先在线路上传。***

而：

```text
Big-Endian / Little-Endian
```

主要讨论：***一个多字节字段中，哪个 byte 在低地址或先传输。***

后面协议链路章节我们还会专门碰到。

---

好，我们进入《系统中的数据表达、编码与传输》的**第四课**。前三课解决的是“数据在内存里到底长什么样”，这一课开始解决一个在嵌入式、密码学、串口调试、网络通信里极其常见的问题：

# 第四章：Raw Binary、Hex String、Base64 —— 同一份数据的三种“外衣”

记住：

> [!important] 先记住
> - **Raw Binary 是数据本身；**
>
> - **Hex 和 Base64 是把二进制数据转换成“适合人类/文本系统传输的字符串”。**
>

```text
同一份逻辑数据
                     │
              ┌──────┴──────┐
              │             │
        Raw Binary       文本表示
         原始字节          │
                          │
                  ┌───────┴───────┐
                  │               │
             Hex String        Base64
```

例如有 2 个真正的字节：

```text
0x01 0x02

# 什么叫真正的字节
# 就是它们在内存中就是：
地址        内容
0x1000      0x01    (0000_0001)
0x1001      0x02    (0000_0002)


这就是 **Raw Binary**
# 写成 Hex 只是方便人读、看
```

但如果我想通过一个只能传文本的系统发送它

就需要编码成 Hex：`"0102"`

```text
"0102"

# 这时就变成了 Hex 字符串（也可以说是 ASCII 字符串）
"0102" = '0''1''0''2''\0'
       = 0x30 0x31 0x30 0x32 0x00

也就是说通过编码：
0x01 0x02 ---> 0x30 0x31 0x30 0x32 0x00
```

注意：

> [!note] 关键区别
> **它们表达的是同样的信息，但存储内容已经完全不同。**

---

## 一、Base64

现在数据：

```text
Raw:
01 A3 FF 20
```

如果转换成 Base64：

```text
AaP/IA==
```

Base64 和 Hex 本质上做的是类似的事情：

- 把任意二进制 byte 转换成一组安全的 ASCII 字符。

- 但它和 Hex 的编码方式不同。

### 为什么需要 Base64？

因为很多系统本质是为**文本**设计的，例如：

```text
JSON
XML
Email
HTTP Header
PEM
某些数据库字段
命令行参数
配置文件
```

如果使用 二进制/Hex 数据会出现：

```text
00
01
FF
0A
0D

0x00 在 C 字符串中可能直接被理解成：字符串结束
0x0A 可能被理解成：换行

所以直接塞进去可能很麻烦
```

Base64 就是：

```text
任意 binary
     ↓
只使用安全 ASCII 字符

Base64 字符表主要由：
        A-Z
        a-z
        0-9
        +
        /
        =
组成

因此非常适合：文本系统里携带二进制数据
```

---

Base64 叫 64，是因为它使用：

```text
64 种主要字符

# 每个字符能够表示：64 种可能
# 那么每一个字符能够表示：log₂64 = 6 bit
# 即：一个 Base64 字符 = 6 bit 信息
#                     = 000 000 ~ 111 111
```

而一个普通 byte：`8 bit`

Base64 会把：

```c
3 bytes
= 24 bits

重新切成
6 + 6 + 6 + 6  // 变成 4 个 Base64 字符
```

所以：

```text
3 bytes Raw
    ↓ Base64
4 chars

Base64 大约膨胀：
4 / 3 ≈ 1.333    // 也就是大约增加 33%
```

因此：

```text
Raw Binary     最紧凑
Base64         +约33%
Hex String     +100%

# 可以记：
Raw
│ 100%
│
Base64
│ 133%
│
Hex String
│ 200%

# 网络 API 里经常更喜欢 Base64，而调试日志更喜欢 Hex（字符串）。
```

---

## 二、调试日志更喜欢 *Hex String* (字符串)

这个时候可能会有一些疑问，比如我要打印  Raw数据: **0xAB**

- 我直接使用 `uart_send()` 发生一个字节直接到 **PC** 串口，串口助手 Hex显示：AB

- 那么为什么说调试日志的时候更喜欢 *Hex String* 也就是字符串？

使用 *Hex String* :

`printf("%02X", 0xAB);`

这个时候，**printf **内部做了一件翻译工作：

它把这 1 个字节，拆成了 2 个独立的 ASCII 字符发送出去

这个时候 **PC** 串口收到的是：41 42  两个字节

然后 **PC** 端使用 ASCII 来解析，在串口助手上显示：AB

数据传输量直接翻倍（膨胀了 100%）

显然使用 Raw 直接发送效率更高

但是在实际的复杂工程调试中，单纯依靠串口助手的全局【Hex 显示】会遇到一些致命痛点：

```text
一、 痛点 1：
    混合日志的“上下文失明”（最核心原因）
# 真实的调试日志，绝不可能只有密文或数据本身，它一定伴随着文本说明、级别标签、时间戳和变量名。

如果使用 Hex String：
    printf("[INFO] SM4 Key is: %02X %02X\n", key[0], key[1]);
ASCII解析屏幕呈现：[INFO] SM4 Key is: 01 23
# 文字描述（[INFO]...）和数据（01 23）完美融合，清晰易读。

如果使用 Raw Bytes
Hex显示：5B 49 4E 46 4F 5D 20 53 4D 34 20 4B 65 79 20 69 73 3A 20 01 23
# 灾难！ 前面的文字提示全变成了 ASCII 码 5B 49 4E...，
# 你为了找末尾的 01 23，必须拿 ASCII 码表去心算前面打印的文字到底是什么！
```

```text
二、 痛点 2：
    字节对齐与排版失控
# 人类肉眼检查，非常依赖固定长度对齐和空格分隔（例如 01 23 45 67 ...）。

如果是【Hex 显示】：
    通常是收到一个字节就硬连着打印一个字节（例如输出 0123456789ABCDEF）
# 没有空格，不区分数据段，也不管你的数组哪里开头、哪里结束。

如果是【Hex String】
    程序员可以用 %02X  自由控制排版
# 如每 16 字节自动换行、中间加空格、加 0x 前缀，极大提升肉眼对比的效率。
```

所以：

```text
调试 / 协议抓包 / 寄存器
→ Hex

文本传输 / JSON / PEM
→ Base64
```

是一个非常常见的规律。

---

## 三、密码学场景中尤其重要

你现在做 SM2、SM4，很容易碰到这种情况。

例如 SM4 key：

```text
0123456789ABCDEFFEDCBA9876543210
```

必须问自己：

> [!warning] 密码学里一定要先确认
> **这是 16 bytes 的 Raw key，还是 32 字节 ASCII Hex String？**

```c
uint8_t key[16];

//     key（Raw key）：0123456789ABCDEFFEDCBA9876543210

// 我们就不能直接：
memcpy(key,
       "0123456789ABCDEFFEDCBA9876543210",    // 文本实际上 32 字节
       16);
```

这样拿到的是：

```text
30 31 32 33 34 35 36 37 ...
```

正确过程应该是：

```text
"0123456789ABCDEFFEDCBA9876543210"（Hex String）
                │
                │ Hex Decode
                ↓
01 23 45 67 89 AB CD EF
FE DC BA 98 76 54 32 10

# 代码里的双引号 " " 括起来的纯十六进制文本，本质上上是 ASCII 字符串（Hex String）
# 是文本

# Hex Decode 是怎么做的
# 以"41" 转换成单个字节 0x41 为例：
输入字符串: "41"
          字符 '4' (ASCII 0x34)  -->  转数值:  4 (二进制: 0000 0100)
          字符 '1' (ASCII 0x31)  -->  转数值:  1 (二进制: 0000 0001)

位运算拼合:
          (4 << 4)  -->  0000 0100 << 4  -->  0100 0000 (0x40)
        |     1     -->                  |    0000 0001 (0x01)
        -----------------------------------------------------
        结果字节                          0100 0001 (0x41)
```

这才是真正：

```text
16-byte SM4 key
```

---

## 四、Formatting

| 写法                          | 内存真实内容（Hex）      | `printf` / 控制台怎么看    | 屏幕显示             |
| --------------------------- | ---------------- | -------------------- | ---------------- |
| `uint8_t a[] = {1, 2};`     | `01 02`          | 当作**数字**打印（`%d`）     | `1 2`            |
| `uint8_t a[] = {'1', '2'};` | `31 32`          | 当作**数字**打印（`%d`）     | `49 50`          |
| `uint8_t a[] = {'1', '2'};` | `31 32`          | 当作**字符**翻译（`%c`）     | `1 2`            |
| `"hello"`                   | `68 65 6C 6C 6F` | 当作**字符串**翻译（`%s`）    | `hello`          |
| `"hello"`                   | `68 65 6C 6C 6F` | 当作**原始十六进制**打印（`%X`） | `68 65 6C 6C 6F` |

**本质只有一条：**

- 内存里永远都是 二进制/十六进制的数值。（十六进制，人为的好看好写）

- 屏幕上显示数字还是显示字母，**完全取决于你告诉 `printf` 用什么视角（格式控制符）去解释这串数据。**

---

## 五、Base64 的长度

Base64 的长度公式可以简单记成：

```c
4 × ceil(N / 3)    // ceil 向上取整，N = 原始 byte 数

// 例如：
1 byte
→ 4 Base64 chars

2 bytes
→ 4 chars

3 bytes
→ 4 chars

4 bytes
→ 8 chars

32 bytes
→ 44 chars
```

其实也就是 `3 byte --> 8 chars`

数据不足 `3 byte` 时，需要 `padding`，例如：

```text
AQ==

# 这里的 =
# 不是数据本身，而是 Base64 padding
```

---

用 `Hello` 看一遍全过程

```text
"Hello"

H    e    l    l    o
48   65   6C   6C   6F

所以 Raw：
    48 65 6C 6C 6F

那么 Hex:
    "48656C6C6F"

Base64:
    SGVsbG8=
```

```text
语义：
Hello

          ↓ ASCII / UTF-8

Raw:
48 65 6C 6C 6F

          ↓ Hex encode

"48656C6C6F"


Raw:
48 65 6C 6C 6F

          ↓ Base64 encode

SGVsbG8=
```

底层永远都是：

```text
byte
```

---

## 六、文件也一样

假设有：

```text
firmware.bin

里面：
    00 20 00 20
    01 03 80 00
    ...
```

如果有人把它转换成：

```text
0020002001038000...
```

然后存成文本文件：

```text
firmware.hex.txt

这就是：Hex String
```

注意，这和 Intel HEX：

```text
firmware.hex
```

还不是同一个概念。

这个以后我们讲**文件格式/容器格式**时会专门讲。

因为 Intel HEX 不是简单：

```text
binary → hex string
```

它还有：

```text
地址
长度
记录类型
校验和

它是一种真正的：file format，不是单纯的 Hex Encode
```

---

## 七、Raw Binary 没有“自解释能力”

例如有：

```text
01 02 03 04
```

我们根本无法知道这是什么

它可能是：

- `uint32_t = 0x04030201`

- 4 个 uint8_t

- RGB \+ Alpha

- 协议头

- 密钥的一部分

- \.\.\.\.

也就是说：**Raw Binary 只有 byte，没有语义。**

语义必须由：

```text
协议
数据结构
文件格式
上下文

# 来提供
```

这一点实际上会直接连接到后面的：

```text
Frame
TLV
ASN.1
DER
协议
```

---

## 八、建立一个“数据六问”

以后遇到任何陌生数据，都问：

```text
① 它是什么语义？
② 它现在是 Raw 还是文本？
③ 如果是文本，是 Hex / Base64 / UTF-8 / 其他？
④ Decode 后多少 byte？
⑤ byte 内部还有没有结构？
⑥ 这个结构使用什么协议/格式解释？
```

例如：

```text
MIIBIjANBgkqh...
```

可以开始：

```text
① 看起来是字符串
② 文本
③ 很像 Base64
④ Base64 decode
⑤ 得到 binary
⑥ binary 可能还是 DER / ASN.1
```

于是：

```text
Base64
≠ 最终数据格式

它只是外面的一层。
```

这正是之后理解 PEM 的关键。

---
